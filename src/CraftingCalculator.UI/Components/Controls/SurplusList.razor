@implements IDisposable

<div class="materials-list">
    @if (State.SurplusStock.Count == 0)
    {
        <div class="pane-empty-state">
            <MudText Typo="Typo.body1">No surplus</MudText>
        </div>
    }
    else
    {
        <MudList T="BlueprintQuantity" Class="record-list" ReadOnly="true">
            @foreach (BlueprintQuantity blueprintQuantity in State.SurplusStock)
            {
                <MudListItem T="BlueprintQuantity">
                    <RecordCard Name="@blueprintQuantity.Name" Kind="@blueprintQuantity.Type.GetDescription()"
                                Record="@blueprintQuantity.Blueprint" Details="@DetailsFor(blueprintQuantity)" />
                </MudListItem>
            }
        </MudList>
    }
</div>
